# V2.1 QA Pass 4 — Delete Submission & Layer Cycle Fixes

## Fixed
- Platform-wide confirmation dialog now preserves the clicked submit button's `name/value` when confirming a form. This fixes POST handlers that depend on names such as `delete_transaction` and `delete_record`.
- Layer Daily Record JavaScript syntax error fixed. The malformed delete function was preventing the entire page script from executing.
- Layer Daily Record active cycle selector now executes normally because the page script parses correctly.
- Layer Daily Record edit buttons and delete buttons now execute normally.
- Layer/Broiler/Ruminant feed transaction deletion now verifies that exactly one transaction row was deleted before committing.
- Ruminant daily record deletion now reports missing/unauthorized records instead of silently redirecting.
- API layer/broiler record deletion now reports when no matching record was actually deleted.

## Validation
- PHP syntax: 56 files checked, 0 failures.
- Contract checks: passed.
- V2 contract checks: passed.
- Native browser `confirm()` calls outside vendor/tests: none found.
